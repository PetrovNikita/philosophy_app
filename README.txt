CommitInfo:
1. на React переписаны : header и комменты. Не понял как передать функционал из jsx-файла, т.к. использую добавление скриптов с реактом и бабелем на html-страницу - нужно указывать mime-тип:
text/babel ; попытался использовать плагин , как написано в интернетах - он не сработал. Поэтому пришлось создать глобальные функции, в которых выполняется ReactDOM.render.

Был в шоке от того, что асинхронные действия можно выполнять только в componentDidMount, componentDidUpdate.

2. отсортировал комменты - сначала поздние.

0. КОММЕНТАРИИ:
1. не пашет показание пароля : type -> text.  

